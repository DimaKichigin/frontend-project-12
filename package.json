{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Chat (Slack)",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "build-frontend": "npm ci && cd frontend && npm ci && npm run build",
    "start": "start-server -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/DimaKichigin/frontend-project-12.git"
  },
  "author": "Dima Kichigin",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^1.1.6"
  }
}
